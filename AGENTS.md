# AI Agent 指令文件

> 本文件适用于所有在本项目中工作的 AI 编码助手（Codex / Claude / GPT / Copilot 等）。
> 读取本文件后，必须严格遵守以下规则。

---

## 一、终止条件（最高优先级）

### 1.1 必须停止的情况

当满足以下**任一**条件时，立即停止开发，输出总结，等待用户确认：

- 用户指定的所有任务已完成
- `npm run verify`（typecheck + test + build）通过
- 已完成 3 次 git commit（达到上限）
- 运行时间超过 2 小时
- 遇到无法自行决策的架构问题

### 1.1.1 Goal 模式的普通阻塞阈值

- 普通、可恢复的环境或执行阻塞，**第一次出现时不得立即终止整个 Goal**
- 同一个阻塞条件连续出现满 **3 个 Goal 回合**，并且仍无法通过安全、任务范围内的替代方案继续推进时，才停止并等待用户确认
- 第 1、2 个阻塞回合应继续完成不依赖该阻塞的工作，并尝试不同的安全诊断或替代路径；禁止为了凑次数重复执行同一失败操作
- 用户恢复一个已阻塞 Goal 后，阻塞计数重新开始；只有恢复后同一条件再次连续出现 3 个回合，才可再次标记为阻塞
- 需要新增用户授权、存在破坏性风险、涉及凭据或外部账号、任务范围不明确、或遇到无法自行决策的架构问题时，仍必须立即停止，不得用“三回合规则”越权继续

### 1.2 禁止的"自我续命"行为

以下行为**严格禁止**：

- ❌ 完成任务后自行发现"可改进点"并继续开发
- ❌ 将 CODE_REVIEW.md 中的建议视为必须完成的任务
- ❌ 在未经用户确认的情况下 bump `package.json` 的 `version` 字段
- ❌ 在未经用户确认的情况下执行 `npm run desktop:dist` 或任何打包命令
- ❌ 修改版本号后自动打包生成 exe
- ❌ 自行创建新的 TODO 任务或开发计划
- ❌ 在完成任务后继续优化代码风格、补充注释、重构无关模块

### 1.3 "完成"的定义

任务"完成"意味着：

1. 指定功能已实现并通过编译
2. 相关测试已编写且通过
3. 相关文档（README.md）已更新
4. `npm run verify` 退出码为 0
5. **停止**，输出变更总结，等待用户反馈

---

## 二、版本管理规则

### 2.1 版本号修改权限

- **只有用户明确说"发布新版本"或"bump 版本"时**，才允许修改 `package.json` 的 `version` 字段
- 修改版本号时，必须同步更新 `desktop/package.json` 的 `version` 字段
- 严禁自行决定从 0.1.0 升到 0.2.0 或任何其他版本号变更

### 2.2 打包权限

- **只有用户明确说"打包"或"生成 exe"时**，才允许执行 `npm run desktop:dist`
- 开发阶段禁止打包，打包只在发布阶段进行
- 打包前必须确认 `release/` 目录中的旧版本是否需要清理

---

## 三、Git 提交规则

### 3.1 提交格式

使用 Conventional Commits 格式：

```
<type>(<scope>): <subject>

<body>
```

- type: `feat` | `fix` | `chore` | `refactor` | `docs` | `test` | `style`
- scope: 可选，如 `desktop`、`server`、`client`
- subject: 简洁描述，不超过 50 字

### 3.2 提交粒度

- 每个提交应该是一个完整的功能单元或修复
- **单次会话最多 3 次提交**，达到上限后停止
- 不要为了"让 Git 历史好看"而拆分提交
- 不要在提交中包含无关的格式化改动

### 3.3 禁止的提交行为

- ❌ 不要在每次小改动后都提交
- ❌ 不要通过提交来"存档"未完成的工作
- ❌ 不要在提交信息中使用"WIP"或"temp"

---

## 四、代码审查报告处理规则

### 4.1 CODE_REVIEW.md 的定位

`CODE_REVIEW.md` 是**供人类阅读的参考文档**，不是 AI 的待办清单。

### 4.2 禁止行为

- ❌ 不要自行实施 CODE_REVIEW.md 中的改进建议
- ❌ 不要将审查报告中的 P0/P1/P2/P3/P4 优先级理解为"必须按顺序完成的任务"
- ❌ 不要在完成用户指定任务后，"顺便"修复审查报告中的问题

### 4.3 正确行为

- 如果用户指定任务与审查报告中的某项建议重合，可以实施
- 如果在开发过程中发现审查报告中提到的 bug，**报告给用户**，不要自行修复
- 审查报告中的建议只有在用户明确说"修复审查报告中的 XXX 问题"时才执行

---

## 五、开发范围控制

### 5.1 只做被要求的事

- 用户说"实现功能 A"，就只实现功能 A
- 不要"顺便"实现功能 B、C、D
- 不要"顺便"优化无关的代码
- 不要"顺便"补充无关的测试

### 5.2 发现额外问题时

如果在开发过程中发现额外问题（bug、安全隐患、性能问题等）：

1. 在最终总结中列出发现的问题
2. **不要自行修复**
3. 等待用户决定是否需要修复

### 5.3 禁止的范围扩展

- ❌ "我发现这个文件也可以优化" → 停下，报告给用户
- ❌ "这个错误处理可以更完善" → 停下，报告给用户
- ❌ "这个测试覆盖率不够" → 停下，报告给用户
- ❌ "这个 UI 可以更好看" → 停下，报告给用户

---

## 六、任务完成后的输出格式

任务完成后，必须输出以下格式的总结，然后停止：

```markdown
## 任务完成总结

### 已完成
- [列出用户指定的每个任务及完成状态]

### 变更文件
- [列出所有修改的文件路径及修改内容简述]

### 测试结果
- npm run verify: [通过/失败]
- 新增测试: [数量]

### 发现的额外问题（未自行修复）
- [列出开发过程中发现但未修复的问题，供用户决策]

### 等待用户确认
以上为本次开发全部内容。请确认是否需要进一步操作。
```

---

## 七、项目特定规则

### 7.1 技术栈

- 后端: Node.js 22 + TypeScript (strict) + Express
- 前端: React 18 + Vite 5 + Tailwind CSS + Zustand
- 持久化: node:sqlite (Node 22 内置)
- 桌面: Electron 43 + electron-builder
- 下载引擎: yt-dlp CLI + FFmpeg

### 7.2 关键约束

- `noUncheckedIndexedAccess` 已开启，数组访问需显式检查 undefined
- node:sqlite 需要 `--experimental-sqlite` 标志
- Windows 环境不支持 SIGSTOP，暂停/恢复使用 kill + `--continue`
- 不用 ORM，直接 SQL + 预编译语句
- 不用 React Router，页面切换用 Zustand view 状态

### 7.3 禁止的技术变更

- ❌ 不要更换 SQLite 驱动（不要改用 better-sqlite3）
- ❌ 不要引入新的前端路由库
- ❌ 不要引入新的状态管理库
- ❌ 不要引入 ORM
- ❌ 不要修改 tsconfig.json 的 strict 相关配置

---

## 八、紧急刹车

如果 AI 在开发过程中出现以下情况，必须立即停止：

1. **重复修改同一文件超过 3 次**（可能是陷入了循环）
2. **执行同一命令超过 2 次失败**（可能是环境问题，需要用户介入）
3. **不确定某个修改是否在任务范围内**（宁可停下问，也不要自作主张）
4. **产生了一个"更好的想法"想要替换现有实现**（先报告，不要直接动手）

---

*本文件最后更新: 2026-08-02*
*创建原因: Codex 在自主 Goal 模式下 6 小时迭代 22 个版本，耗尽一周额度*
